{{- if .Values.ingress.enabled }}
{{- range $host := .Values.ingress.hosts }}
{{- range .paths }}
http{{ if $host.tls }}s{{ end }}://{{ $host.host }}{{ .path }}
{{- end }}
{{- end }}
{{- else }}
Use port-forwarding:
kubectl -n {{ .Release.Namespace }} port-forward svc/{{ include "quake.fullname" . }} 8080:{{ .Values.service.port }}
{{- end }}
